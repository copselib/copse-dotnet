// <auto-generated>
//   Generated from AsyncTreenumerable.WithPosition.cs by Copse.CodeGen (async->sync transcription).
//   Do not edit; edit the async source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
using Copse.Core;
using System;

namespace Copse.Linq
{
  public static partial class Treenumerable
  {
    /// <summary>
    /// Pairs each node with its position in THIS tree, at THIS point in the chain --
    /// positionality as a SPELLING (design-docs/WITHPOSITION_DESIGN.md): downstream
    /// operators compose over the <see cref="NodeContext{TNode}"/> pair with their VALUE
    /// flavors, and the whole positional grammar is sugar over this one operator.
    ///
    /// <para>Capture scope is explicit and visible in the spelling: the pair carries the
    /// EMITTED labels of this operator's input. A relabeling operator downstream
    /// (<c>Where</c>, <c>PruneBefore</c>) does not update captured pairs -- they are data,
    /// not coordinates; call <c>WithPosition</c> again after the mutation to re-capture.
    /// (The join rule as data flow: today's guard machinery guarantees exactly this
    /// reading for positional lambdas; here it is true by construction.)</para>
    ///
    /// <para>Deferred, streaming, and machinery-free: this IS the positional projection --
    /// it rides the light Select machinery, composes in-tier over label-preserving chains,
    /// and stacks over relabeling ones, exactly as any positional selector does. In a
    /// collapsed chain the pair is minted transiently inside the composed selector (the
    /// emission mint) -- <c>WithPosition().PruneAfter(...).Select(nc =&gt; nc.Node)</c>
    /// costs one wrapper and no stored pairs.</para>
    /// </summary>
    public static ITreenumerable<NodeContext<TNode>> WithPosition<TNode>(
      this ITreenumerable<TNode> source)
      => source.Select((node, position) => new NodeContext<TNode>(node, position));

    /// <summary>The depth-first narrow flavor; see the composite overload.</summary>
    public static IDepthFirstTreenumerable<NodeContext<TNode>> WithPosition<TNode>(
      this IDepthFirstTreenumerable<TNode> source)
      => source.Select((node, position) => new NodeContext<TNode>(node, position));

    /// <summary>The breadth-first narrow flavor; see the composite overload.</summary>
    public static IBreadthFirstTreenumerable<NodeContext<TNode>> WithPosition<TNode>(
      this IBreadthFirstTreenumerable<TNode> source)
      => source.Select((node, position) => new NodeContext<TNode>(node, position));
  }
}
