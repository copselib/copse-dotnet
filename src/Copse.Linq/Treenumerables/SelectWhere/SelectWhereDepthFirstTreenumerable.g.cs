// <auto-generated>
//   Generated from AsyncSelectWhereDepthFirstTreenumerable.g.cs by Copse.CodeGen (async->sync transcription).
//   Do not edit; edit the async source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
// <auto-generated>
//   Generated from AsyncSelectWhereTreenumerable.cs by Copse.CodeGen (composite->narrow transcription).
//   Do not edit; edit the composite-width source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
using Copse.Core;
using Copse.Linq.Treenumerators;
using System;

namespace Copse.Linq.Treenumerables
{
  // The reified operator chain (design-docs/OPERATOR_COMPOSITION_DESIGN.md, "the result monad"): one wrapper
  // holding the Kleisli-composed result of every composed operator, so chains of any
  // length and order collapse to ONE layer over the source. Plain operators
  // instantiate with their bespoke selector STRUCT (inlined by the JIT -- zero seam cost);
  // composed chains nest those structs in the TYPE via ComposedResultSelector (a user
  // delegate enters only as a FuncResultSelector leaf). Splicing is total: every legality
  // decision was made outer-side.
  internal sealed partial class SelectWhereDepthFirstTreenumerable<TSource, TResult, TResultSelector> : ISelectWhereDepthFirstTreenumerable<TResult>
    where TResultSelector : struct, IResultSelector<TSource, TResult>
  {
    public SelectWhereDepthFirstTreenumerable(
      IDepthFirstTreenumerable<TSource> source,
      TResultSelector resultSelector)
    {
      _Source = source;
      _ResultSelector = resultSelector;
    }

    private readonly IDepthFirstTreenumerable<TSource> _Source;
    private readonly TResultSelector _ResultSelector;

    public ITreenumerator<TResult> GetDepthFirstTreenumerator() =>
      new WhereDepthFirstTreenumerator<TSource, TResult, TResultSelector>(
        _Source.GetDepthFirstTreenumerator, _ResultSelector);

    // The composition law under this representation's successor choice: the general
    // wrapper composes to a general wrapper, and the chain nests in the type -- every leg
    // inlinable.
    public IDepthFirstTreenumerable<TOuterResult> Compose<TOuterResult, TOuterSelector>(
      TOuterSelector outerSelector,
      bool relabels)
      where TOuterSelector : struct, IResultSelector<TResult, TOuterResult>
      => Splice<TOuterResult, TOuterSelector>(outerSelector);

    // The context-shaped projection door: the projection rides an inlinable struct leg
    // (the caller has already applied the join rule for positional legs).
    public IDepthFirstTreenumerable<TOuterResult> Compose<TOuterResult>(Func<NodeContext<TResult>, TOuterResult> selector)
      => Splice<TOuterResult, SelectResultSelector<TResult, TOuterResult>>(
        new SelectResultSelector<TResult, TOuterResult>(selector));

    // THE ONE CONSTRUCTION of a composed successor: every door that splices a leg lands
    // here, so the nested-selector spelling has a single home. It returns the concrete type
    // because an interface implementation cannot -- which is what lets the public
    // projection door (the citizenship part) reuse it rather than respell it.
    private SelectWhereDepthFirstTreenumerable<TSource, TOuterResult, ComposedResultSelector<TSource, TResult, TOuterResult, TResultSelector, TOuterSelector>> Splice<TOuterResult, TOuterSelector>(
      TOuterSelector outerSelector)
      where TOuterSelector : struct, IResultSelector<TResult, TOuterResult>
      => new SelectWhereDepthFirstTreenumerable<TSource, TOuterResult, ComposedResultSelector<TSource, TResult, TOuterResult, TResultSelector, TOuterSelector>>(
        _Source,
        new ComposedResultSelector<TSource, TResult, TOuterResult, TResultSelector, TOuterSelector>(_ResultSelector, outerSelector));

    // ---- The position-reading doors: this driver STACKS ----
    //
    // A spliced leg is evaluated against this driver's INNER coordinates, which are not the
    // ones it publishes -- promotion has moved them. So a position-reading leg goes into a
    // wrapper over this driver, where it reads published labels by construction. Nothing is
    // consulted: a driver exists because something rejected, and rejection is what moves
    // labels.
    public IDepthFirstTreenumerable<TOuterResult> ComposePositional<TOuterResult>(Func<NodeContext<TResult>, TOuterResult> selector)
      => new SelectDepthFirstTreenumerable<TResult, TOuterResult>(this, selector);

    public IDepthFirstTreenumerable<TOuterResult> ComposePositional<TOuterResult, TOuterSelector>(
      TOuterSelector outerSelector,
      bool relabels)
      where TOuterSelector : struct, IResultSelector<TResult, TOuterResult>
      => new SelectWhereDepthFirstTreenumerable<TResult, TOuterResult, TOuterSelector>(this, outerSelector);

    // The context-shaped prune-after door: the in-tier-only boundary ruling (2026-08-04,
    // the surviving half) -- the light prune wrapper STACKS over the driver rather than
    // demoting its representation for a layer that costs almost nothing.
    public IDepthFirstTreenumerable<TResult> ComposePruneAfter(Func<NodeContext<TResult>, bool> predicate)
      => new PruneAfterDepthFirstTreenumerable<TResult>(this, predicate);
  }
}
