// <auto-generated>
//   Generated from AsyncWhereTreenumerable.cs by Copse.CodeGen (async->sync transcription).
//   Do not edit; edit the async source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
using Copse.Core;
using Copse.Linq.Treenumerators;
using System;

namespace Copse.Linq.Treenumerables
{
  // The value-only Where wrapper: the named, probeable form. Only the value-only overload
  // builds this (the positional overload builds the anonymous factory wrapper and never fuses
  // with its own kind -- each positional predicate is entitled to its input tree's labels, and
  // a Where boundary relabels). An appended value-only Where fuses by predicate combination --
  // LINQ's CombinePredicates, legal because neither predicate observes a coordinate.
  internal sealed class WhereTreenumerable<TNode> : IFusableTreenumerable<TNode>
  {
    public WhereTreenumerable(ITreenumerable<TNode> source, Func<TNode, bool> predicate)
    {
      _Source = source;
      _Predicate = predicate;
      _ContextPredicate = nodeContext => predicate(nodeContext.Node);
    }

    private readonly ITreenumerable<TNode> _Source;
    private readonly Func<TNode, bool> _Predicate;
    private readonly Func<NodeContext<TNode>, bool> _ContextPredicate;

    public ITreenumerator<TNode> GetBreadthFirstTreenumerator() =>
      new WhereBreadthFirstTreenumerator<TNode, TNode>(
        _Source.GetBreadthFirstTreenumerator,
        IdentitySelector<TNode>.Instance,
        _ContextPredicate,
        NodeTraversalStrategies.SkipNode);

    public ITreenumerator<TNode> GetDepthFirstTreenumerator() =>
      new WhereDepthFirstTreenumerator<TNode, TNode>(
        _Source.GetDepthFirstTreenumerator,
        IdentitySelector<TNode>.Instance,
        _ContextPredicate,
        NodeTraversalStrategies.SkipNode);

    public ITreenumerable<TNode> FuseWhere(Func<TNode, bool> predicate)
    {
      var innerPredicate = _Predicate;

      return new WhereTreenumerable<TNode>(_Source, node => innerPredicate(node) && predicate(node));
    }

    // This boundary relabels (depth compression, sibling renumbering), and a positional
    // predicate is entitled to the relabeled coordinates.
    public ITreenumerable<TNode> FusePositionalWhere(Func<TNode, NodePosition, bool> predicate) => null;

    // Where-then-Select needs an emission-side projection seam the drivers do not have yet
    // (they project BEFORE the test); decline until that seam exists.
    public ITreenumerable<TOuterResult> FuseSelect<TOuterResult>(Func<NodeContext<TNode>, TOuterResult> selector) => null;
  }
}
