// <auto-generated>
//   Generated from IAsyncProjectionSource.cs by Copse.CodeGen (async->sync transcription).
//   Do not edit; edit the async source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
using Copse.Core;
using System;

namespace Copse.Linq.Treenumerables
{
  // COMPOSE-LEFT's door (SELECT_INTO_CAPTURES_DESIGN.md section 3): Compose's INVERSE verb,
  // living on the same wrapper. Compose is same-kind succession -- "here is more mapping,
  // build your successor" -- and its floor is one wrapper on the walk, because its output is
  // always a treenumerable. This door is cross-kind surrender: a consumer of a DIFFERENT
  // kind (a scan's capture pass) asks the wrapper for its PIECES -- the un-projected inner
  // tree and the projector -- so the consumer can walk the inner RAW, zero wrappers, and
  // absorb the projection into its own iteration.
  //
  // The double-dispatch shape exists because the wrapper's inner type is existential: at the
  // consumer's site TInner is unnameable (no wildcard generics), so the consumer supplies a
  // generic method and the WRAPPER instantiates it -- the one scope where TInner can be
  // spelled. Only the pure-projection wrapper claims this door: a filter-carrying driver
  // cannot surrender (rejections change the tree the consumer would walk). Deliberately
  // INTERNAL, like the composition lattice: the public citizenship composes rightward;
  // compose-left reads our own wrappers' insides, which no foreign type can be trusted to
  // report honestly.
  internal interface IProjectionSource<TProjected>
  {
    /// <summary>Surrender the pieces: calls <paramref name="consumer"/> back with the
    /// un-projected inner source and the projector, instantiating the consumer's TInner.</summary>
    TResult CaptureThrough<TResult>(IProjectionConsumer<TProjected, TResult> consumer);
  }

  /// <summary>The consumer half of the door: a builder of some OTHER kind (a scan, a capture)
  /// that can do its work over any inner type, given the projector into its own domain.</summary>
  internal interface IProjectionConsumer<TProjected, TResult>
  {
    TResult Consume<TInner>(ITreenumerable<TInner> innerSource, Func<NodeAndPosition<TInner>, TProjected> projector);
  }
}
