// <auto-generated>
//   Generated from AsyncScanWhereBreadthFirstTreenumerator.cs by Copse.CodeGen (async->sync transcription).
//   Do not edit; edit the async source and regenerate: dotnet run --project Copse.CodeGen
// </auto-generated>
using Copse.Core;
using Copse.Linq.Treenumerables;
using Copse.Linq.Extensions;
using Copse.Linq.Treenumerators; // WhereBreadthFirstPath (internal, via InternalsVisibleTo)
using System;
using System.Collections.Generic;

namespace Copse.Linq.Treenumerators
{
  /// <summary>
  /// The FOURTH CELL's breadth-first machine (the ancestor composer, SCAN_TIER_DESIGN.md) and
  /// the codegen source of truth for its sync twin: the BFT filter driver with an
  /// inherited-fold stage. The filter half is
  /// <see cref="WhereBreadthFirstTreenumerator{TSource, TResult, TResultSelector}"/> verbatim;
  /// the fold half is THE ACCUMULATE TRACKER -- the rootfix BFT engine's state machinery
  /// (level deques, skipped stack, the four-way parent lookup) carrying bare accumulates,
  /// minus that engine's emission. Both halves read the same inner pulls; the composed
  /// selector chain sees the pair <c>(node, accumulate)</c> minted transiently at the
  /// decision site.
  ///
  /// <para>THE REJECTION BRIDGE: a predicate-rejected node's SkipNode goes to the INNER (the
  /// driver's normal promotion protocol), which means the inner emits no visits for it -- so
  /// the tracker treats driver rejections exactly as the rootfix engine treats consumer
  /// SkipNode: the rejected node's accumulate moves to the tracker's skipped stack, where
  /// the parent lookup finds it when the node's children schedule (composition is data flow:
  /// descendants fold THROUGH rejected ancestors). The engine solved this correlation
  /// problem years of pins ago; this machine inherits the solution verbatim.</para>
  /// </summary>
  internal sealed class ScanWhereBreadthFirstTreenumerator<TSource, TAccumulate, TResult, TResultSelector>
    : TreenumeratorWrapper<TSource, TResult>
    where TResultSelector : struct, IResultSelector<NodeAccumulation<TSource, TAccumulate>, TResult>
  {
    public ScanWhereBreadthFirstTreenumerator(
      Func<ITreenumerator<TSource>> innerTreenumeratorFactory,
      Func<NodeContext<TAccumulate>, NodeContext<TSource>, TAccumulate> accumulator,
      TAccumulate seed,
      TResultSelector resultSelector)
      : base(innerTreenumeratorFactory)
    {
      _Accumulator = accumulator;
      _ResultSelector = resultSelector;

      _TrackerCurrentLevel = new RefSemiDeque<NodeContext<TAccumulate>>();
      _TrackerNextLevel = new RefSemiDeque<NodeContext<TAccumulate>>();
      _TrackerSkippedStack = new Stack<NodeContext<TAccumulate>>();

      // The tracker's sentinel: the virtual forest root carrying the seed (the scan
      // engines' exact boundary).
      _TrackerCurrentLevel.AddLast(new NodeContext<TAccumulate>(seed, NodePosition.ForestRoot));

      _Path = new WhereBreadthFirstPath<TResult>(default, InnerTreenumerator.Position);
    }

    private readonly Func<NodeContext<TAccumulate>, NodeContext<TSource>, TAccumulate> _Accumulator;
    private readonly TResultSelector _ResultSelector;

    // ---- The accumulate tracker: the rootfix BFT engine's state, bare accumulates ----
    private RefSemiDeque<NodeContext<TAccumulate>> _TrackerCurrentLevel;
    private RefSemiDeque<NodeContext<TAccumulate>> _TrackerNextLevel;
    private readonly Stack<NodeContext<TAccumulate>> _TrackerSkippedStack;
    private bool _TrackerScheduledChildrenSinceSkip = false;

    // Fold the just-scheduled inner node from its parent's accumulate (the engine's four-way
    // parent lookup, verbatim), record it in the next level, and return it.
    private TAccumulate TrackerOnScheduling()
    {
      var parentDepth = InnerTreenumerator.Position.Depth - 1;

      while (_TrackerSkippedStack.Count > 0
        && _TrackerSkippedStack.Peek().Position.Depth != parentDepth)
      {
        _TrackerSkippedStack.Pop();
      }

      if (InnerTreenumerator.Position.SiblingIndex == 0
        && _TrackerScheduledChildrenSinceSkip
        && _TrackerSkippedStack.Count > 0)
      {
        _TrackerSkippedStack.Pop();
        _TrackerScheduledChildrenSinceSkip = false;
      }

      NodeContext<TAccumulate> parentContext;
      if (_TrackerCurrentLevel.GetFirst().Position.Depth == parentDepth)
        parentContext = _TrackerCurrentLevel.GetFirst();
      else if (_TrackerSkippedStack.Count > 0)
        parentContext = _TrackerSkippedStack.Peek();
      else if (_TrackerNextLevel.Count > 0 && _TrackerNextLevel.GetFirst().Position.Depth == parentDepth)
        parentContext = _TrackerNextLevel.GetFirst();
      else
        parentContext = _TrackerCurrentLevel.GetFirst();

      var accumulate = _Accumulator(parentContext, InnerTreenumerator.ToNodeContext());

      _TrackerNextLevel.AddLast(new NodeContext<TAccumulate>(accumulate, InnerTreenumerator.Position));

      _TrackerScheduledChildrenSinceSkip = true;

      return accumulate;
    }

    // A driver rejection is the tracker's SkipNode: the node's visits will never arrive from
    // the inner, so its accumulate moves to the skipped stack for its children's lookups.
    private void TrackerOnRejected()
    {
      _TrackerSkippedStack.Push(_TrackerNextLevel.RemoveLast());
      _TrackerScheduledChildrenSinceSkip = false;
    }

    // The inner visits a node (first visit): advance the tracker's level cadence -- the
    // engine's OnVisitingNode bookkeeping, minus emission.
    private void TrackerOnFirstVisit()
    {
      _TrackerSkippedStack.Clear();
      _TrackerCurrentLevel.RemoveFirst();

      if (_TrackerCurrentLevel.Count == 0)
        (_TrackerCurrentLevel, _TrackerNextLevel) = (_TrackerNextLevel, _TrackerCurrentLevel);

      while (_TrackerCurrentLevel.Count > 0
        && _TrackerCurrentLevel.GetFirst().Position != InnerTreenumerator.Position)
      {
        _TrackerCurrentLevel.RemoveFirst();
      }
    }

    // ---- The filter half: WhereBreadthFirstTreenumerator, verbatim ----

    private WhereBreadthFirstPath<TResult> _Path;

    private enum FrontReturnVisit
    {
      None,
      Owed,
      SuppressNextInner,
    }

    private FrontReturnVisit _FrontReturnVisit = FrontReturnVisit.None;

    private NodeTraversalStrategies? _DeferredStrategy = null;

    private NodeTraversalStrategies _PendingResultStrategies = NodeTraversalStrategies.TraverseAll;
    private NodeTraversalStrategies _DeferredResultStrategies = NodeTraversalStrategies.TraverseAll;

    protected override bool OnMoveNext(NodeTraversalStrategies nodeTraversalStrategies)
    {
      if (_Path.ConsumerSkipDeferredSchedulePending)
      {
        _Path.ClearConsumerSkippedSubtree();
        Publish(ref _Path.Back, TreenumeratorMode.SchedulingNode);
        _PendingResultStrategies = _DeferredResultStrategies;
        _DeferredResultStrategies = NodeTraversalStrategies.TraverseAll;
        return true;
      }

      nodeTraversalStrategies |= _PendingResultStrategies;
      _PendingResultStrategies = NodeTraversalStrategies.TraverseAll;

      if (Mode == TreenumeratorMode.VisitingNode)
      {
        if (_DeferredStrategy.HasValue)
        {
          nodeTraversalStrategies = _DeferredStrategy.Value;
          _DeferredStrategy = null;
        }
        else
        {
          nodeTraversalStrategies = NodeTraversalStrategies.TraverseAll;
        }
      }

      var previouslySeenNodeWasScheduledAndSkipped =
        !Position.IsForestRoot
        && InnerTreenumerator.Mode == TreenumeratorMode.SchedulingNode
        && nodeTraversalStrategies.HasNodeTraversalStrategies(NodeTraversalStrategies.SkipNode);

      if (previouslySeenNodeWasScheduledAndSkipped)
      {
        _Path.Back.TraversalStrategy = nodeTraversalStrategies;

        // The consumer-skip bridge (the engine's own consumer-SkipNode handling, applied to
        // the tracker): the node's inner visits will never arrive. Plain SkipNode keeps its
        // descendants (they look the accumulate up on the skipped stack); PruneSubtree
        // drops the whole subtree (nothing will ever look it up).
        if (nodeTraversalStrategies.HasNodeTraversalStrategies(NodeTraversalStrategies.PruneSubtree))
          _TrackerNextLevel.RemoveLast();
        else
          TrackerOnRejected();
      }

      if (_FrontReturnVisit == FrontReturnVisit.Owed && !previouslySeenNodeWasScheduledAndSkipped)
      {
        if (_Path.Front.Position.Depth >= 0)
        {
          _FrontReturnVisit = FrontReturnVisit.None;
          _DeferredStrategy = nodeTraversalStrategies;
          _Path.Front.VisitCount++;
          Publish(ref _Path.Front, TreenumeratorMode.VisitingNode);
          return true;
        }
      }

      if (previouslySeenNodeWasScheduledAndSkipped)
      {
        if (_FrontReturnVisit == FrontReturnVisit.Owed)
          _FrontReturnVisit = FrontReturnVisit.SuppressNextInner;

        if (_Path.ConsumerSkippedSubtreeDepth >= 0)
          _Path.MarkConsumerSkippedChildAfterLastAccepted();
      }

      if (_FrontReturnVisit == FrontReturnVisit.Owed)
        _FrontReturnVisit = FrontReturnVisit.None;

      while (InnerTreenumerator.MoveNext(nodeTraversalStrategies))
      {
        if (InnerTreenumerator.Mode == TreenumeratorMode.SchedulingNode)
        {
          var innerDepth = InnerTreenumerator.Position.Depth;

          // THE FOLD STAGE, before the accept/reject decision (composition is data flow).
          var accumulate = TrackerOnScheduling();

          // ONE evaluation of the composed selector chain, against the PAIR context.
          var result = _ResultSelector.GetResult(
            new NodeContext<NodeAccumulation<TSource, TAccumulate>>(
              new NodeAccumulation<TSource, TAccumulate>(InnerTreenumerator.Node, accumulate),
              InnerTreenumerator.Position));
          var skipped = result.Strategies.HasNodeTraversalStrategies(NodeTraversalStrategies.SkipNode);
          _Path.PrefixWriteForScheduledNode(innerDepth, skipped);

          if (skipped)
          {
            // The rejected node's inner visits will never arrive: bridge its accumulate to
            // the tracker's skipped stack for its children's lookups.
            TrackerOnRejected();
            nodeTraversalStrategies = result.Strategies;
            continue;
          }


          // --- Accepted node processing ---
          var lastScheduleNodeVisitWasSkipped = _Path.Back.Skipped;
          var removedSkippedDepth = lastScheduleNodeVisitWasSkipped
            ? _Path.Back.Position.Depth
            : -1;

          if (lastScheduleNodeVisitWasSkipped)
          {
            _Path.BeginRemovedSkippedParent(removedSkippedDepth);
          }

          var effectivePosition = _Path.GetEffectivePosition(innerDepth, out var immediateParentIsSkipped);
          _Path.RecordAcceptedChild(effectivePosition);

          _FrontReturnVisit = immediateParentIsSkipped && effectivePosition.Depth > 0
            ? FrontReturnVisit.Owed
            : FrontReturnVisit.None;

          if (_Path.ConsumerSkippedSubtreeDepth >= 0
            && effectivePosition.Depth > _Path.ConsumerSkippedSubtreeDepth)
            _Path.ClearConsumerSkippedChildAfterLastAccepted();

          if (lastScheduleNodeVisitWasSkipped && removedSkippedDepth >= 0
            && effectivePosition.Depth > removedSkippedDepth)
          {
            _Path.EnterConsumerSkippedSubtree(removedSkippedDepth);
          }
          else if (_Path.ConsumerSkippedSubtreeDepth >= 0
            && effectivePosition.Depth <= _Path.ConsumerSkippedSubtreeDepth
            && effectivePosition.Depth > 0
            && Mode != TreenumeratorMode.VisitingNode
            && _Path.Front.Position.Depth >= 0)
          {
            if (!_Path.ConsumerSkippedChildAfterLastAccepted)
            {
              _Path.ClearConsumerSkippedChildAfterLastAccepted();
              _Path.EnqueueAccepted(result.Node, effectivePosition, innerDepth);
              _Path.MarkDeferredSchedulePending();
              _DeferredResultStrategies = result.Strategies;
              _Path.Front.VisitCount++;
              Publish(ref _Path.Front, TreenumeratorMode.VisitingNode);
              return true;
            }

            _Path.ClearConsumerSkippedSubtree();
            _Path.ClearConsumerSkippedChildAfterLastAccepted();
          }

          _Path.EnqueueAccepted(result.Node, effectivePosition, innerDepth);
          _PendingResultStrategies = result.Strategies;
        }
        else // VisitingNode
        {
          if (InnerTreenumerator.VisitCount == 1)
            TrackerOnFirstVisit();

          if (_FrontReturnVisit == FrontReturnVisit.SuppressNextInner
            && InnerTreenumerator.VisitCount > 1)
          {
            _FrontReturnVisit = FrontReturnVisit.None;
            if (_Path.Front.VisitCount >= InnerTreenumerator.VisitCount)
              continue;
          }

          if (InnerTreenumerator.VisitCount == 1)
          {
            _Path.RetireFrontAndReanchor();
          }
          else if (Mode == TreenumeratorMode.VisitingNode)
            continue;

          _Path.Front.VisitCount++;
        }

        Publish(ref _Path.SelectPublishFrame(InnerTreenumerator.Mode == TreenumeratorMode.SchedulingNode), InnerTreenumerator.Mode);
        return true;
      }

      Publish(ref _Path.SelectPublishFrame(InnerTreenumerator.Mode == TreenumeratorMode.SchedulingNode), InnerTreenumerator.Mode);
      return false;
    }

    private void Publish(ref WhereBreadthFirstPath<TResult>.AcceptedFrame frame, TreenumeratorMode mode)
    {
      Mode = mode;
      Node = frame.Node;
      VisitCount = frame.VisitCount;
      Position = frame.Position;
    }
  }
}
